[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "auditlens"
version = "1.0.0"
description = "Financial anomaly detection and audit analytics over a synthetic journal ledger"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "AuditLens" }]
keywords = [
    "audit-analytics",
    "benford",
    "isolation-forest",
    "anomaly-detection",
    "internal-audit",
    "data-science",
]
classifiers = [
    "Programming Language :: Python :: 3.11",
    "Topic :: Office/Business :: Financial :: Accounting",
    "Intended Audience :: Financial and Insurance Industry",
]
dependencies = [
    "pandas>=3.0,<4.0",
    "numpy>=2.0,<3.0",
    "scikit-learn>=1.5,<2.0",
    "scipy>=1.13,<2.0",
    "matplotlib>=3.9,<4.0",
    "plotly>=6.0,<8.0",
    "streamlit>=1.40,<2.0",
    "sqlalchemy>=2.0,<3.0",
    "pyarrow>=17.0,<26.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0,<10.0", "pytest-cov>=5.0,<8.0", "ruff==0.16.5"]

[tool.setuptools]
packages = ["src", "dashboard", "dashboard.views"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --strict-markers"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["src", "dashboard"]
omit = ["tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# These entry points adjust sys.path or configure Streamlit before importing
# the rest of the application, so their delayed imports are intentional.
"dashboard/app.py" = ["E402"]
"dashboard/common.py" = ["E402"]
"tests/conftest.py" = ["E402"]
"tests/test_dashboard.py" = ["E402"]
"tests/test_reporting.py" = ["E402"]
"tests/test_utils.py" = ["E402"]
"tools/build_notebooks.py" = ["E402"]
